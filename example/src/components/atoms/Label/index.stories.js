import React from 'react'
import { storiesOf } from '@storybook/react'

import { Label } from '@heapapp/ui'

storiesOf('Form', module).add('Label', () => (
   <div>
      <Label>Label</Label>
   </div>
))
